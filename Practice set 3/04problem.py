#Replace the double space from problem 3 with single spaces in the string

name = "Ram is  boy"
print (name.replace("  ", " "))

print (name) #Strings are imutable which means that you cannot change them by running functions on them. You have to assign the result of the function to a new variable.
